name: Continuous Delivery

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read
  packages: write

jobs:

  # --------------------------------------------------
  # 1. Test
  # --------------------------------------------------
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          pytest


  # --------------------------------------------------
  # 2. Build and Push Docker Image
  # --------------------------------------------------
  build:
    name: Build and Push Docker Image
    needs: test
    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract version from Git tag
        id: version
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

          echo "Git tag: $GITHUB_REF_NAME"
          echo "Docker image version: $VERSION"

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build \
            -t ghcr.io/${{ github.repository_owner }}/mlops_activity_1:${{ steps.version.outputs.version }} \
            -t ghcr.io/${{ github.repository_owner }}/mlops_activity_1:latest \
            .

      - name: Push versioned Docker image
        run: |
          docker push \
            ghcr.io/${{ github.repository_owner }}/mlops_activity_1:${{ steps.version.outputs.version }}

      - name: Push latest Docker image
        run: |
          docker push \
            ghcr.io/${{ github.repository_owner }}/mlops_activity_1:latest